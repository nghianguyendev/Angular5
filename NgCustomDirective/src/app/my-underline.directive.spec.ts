import { MyUnderlineDirective } from './my-underline.directive';

describe('MyUnderlineDirective', () => {
  it('should create an instance', () => {
    const directive = new MyUnderlineDirective();
    expect(directive).toBeTruthy();
  });
});
